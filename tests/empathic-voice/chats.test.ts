/**
 * This file was auto-generated by Fern from our API Definition.
 */

import { HumeClient } from "../../src/Client";

const client = new HumeClient({
    environment: process.env.TESTS_BASE_URL || "test",
    apiKey: process.env.TESTS_AUTH || "test",
});

describe("Chats", () => {
    test("listChats", async () => {
        const response = await client.empathicVoice.chats.listChats();
        expect(response).toEqual({
            pageNumber: 1,
            pageSize: 1,
            chatsPage: [
                {
                    id: "id",
                    chatGroupId: "chat_group_id",
                    tag: "tag",
                    status: "status",
                    startTimestamp: 1,
                    endTimestamp: 1,
                    eventCount: 1,
                    metadata: "metadata",
                    config: { id: "id" },
                },
            ],
        });
    });

    test("listChatEvents", async () => {
        const response = await client.empathicVoice.chats.listChatEvents("id");
        expect(response).toEqual({
            id: "id",
            chatGroupId: "chat_group_id",
            tag: "tag",
            status: "status",
            startTimestamp: 1,
            endTimestamp: 1,
            eventsPage: [
                {
                    id: "id",
                    chatId: "chat_id",
                    timestamp: 1,
                    role: "role",
                    type: "type",
                    messageText: "message_text",
                    emotionFeatures: "emotion_features",
                    metadata: "metadata",
                },
            ],
            metadata: "metadata",
            pageNumber: 1,
            pageSize: 1,
            config: { id: "id", version: 1 },
        });
    });
});
