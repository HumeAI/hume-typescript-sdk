/**
 * This file was auto-generated by Fern from our API Definition.
 */

import { HumeClient } from "../../src/Client";

const client = new HumeClient({
    environment: process.env.TESTS_BASE_URL || "test",
    apiKey: process.env.TESTS_AUTH || "test",
});

describe("ChatGroups", () => {
    test("listChatGroups", async () => {
        const response = await client.empathicVoice.chatGroups.listChatGroups();
        expect(response).toEqual({
            pageNumber: 1,
            pageSize: 1,
            chatGroupsPage: [
                {
                    id: "id",
                    firstStartTimestamp: 1,
                    mostRecentStartTimestamp: 1,
                    mostRecentChatId: "most_recent_chat_id",
                    numChats: 1,
                    active: true,
                },
            ],
        });
    });

    test("listChatGroupEvents", async () => {
        const response = await client.empathicVoice.chatGroups.listChatGroupEvents("id");
        expect(response).toEqual({
            id: "id",
            pageNumber: 1,
            pageSize: 1,
            paginationDirection: "pagination_direction",
            eventsPage: [
                {
                    id: "id",
                    chatId: "chat_id",
                    timestamp: 1,
                    role: "role",
                    type: "type",
                    messageText: "message_text",
                    emotionFeatures: "emotion_features",
                    metadata: "metadata",
                },
            ],
        });
    });
});
