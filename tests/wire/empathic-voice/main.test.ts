/**
 * This file was auto-generated by Fern from our API Definition.
 */

import { mockServerPool } from "../../mock-server/MockServerPool";
import { HumeClient } from "../../../src/Client";
import * as Hume from "../../../src/api/index";

describe("EmpathicVoice", () => {
    test("send_message_v0_evi_chat__chat_id__send_post (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new HumeClient({
            apiKey: "test",
            environment: { base: server.baseUrl, evi: server.baseUrl, tts: server.baseUrl, stream: server.baseUrl },
        });
        const rawRequestBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/v0/evi/chat/chat_id/send")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.empathicVoice.sendMessageV0EviChatChatIdSendPost("chat_id", {
            body: {
                key: "value",
            },
        });
        expect(response).toEqual(undefined);
    });

    test("send_message_v0_evi_chat__chat_id__send_post (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new HumeClient({
            apiKey: "test",
            environment: { base: server.baseUrl, evi: server.baseUrl, tts: server.baseUrl, stream: server.baseUrl },
        });
        const rawRequestBody = { key: "value" };
        const rawResponseBody = { detail: undefined };
        server
            .mockEndpoint()
            .post("/v0/evi/chat/chat_id/send")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(422)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.empathicVoice.sendMessageV0EviChatChatIdSendPost("chat_id", {
                body: {
                    key: "value",
                },
            });
        }).rejects.toThrow(
            new Hume.empathicVoice.UnprocessableEntityError({
                detail: undefined,
            }),
        );
    });
});
