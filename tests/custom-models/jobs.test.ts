/**
 * This file was auto-generated by Fern from our API Definition.
 */

import { HumeClient } from "../../src/Client";

const client = new HumeClient({
    environment: process.env.TESTS_BASE_URL || "test",
    apiKey: process.env.TESTS_AUTH || "test",
});

describe("Jobs", () => {
    test("startTrainingJob", async () => {
        const response = await client.customModels.jobs.startTrainingJob({
            customModel: {
                name: "name",
            },
            dataset: {
                id: "id",
            },
        });
        expect(response).toEqual({ jobId: "job_id" });
    });

    test("startCustomModelsInferenceJob", async () => {
        const response = await client.customModels.jobs.startCustomModelsInferenceJob({
            customModel: {
                id: "id",
            },
        });
        expect(response).toEqual({ jobId: "job_id" });
    });
});
