/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as environments from "../../../../environments.js";
import * as core from "../../../../core/index.js";
import * as Hume from "../../../index.js";
import { mergeHeaders, mergeOnlyDefinedHeaders } from "../../../../core/headers.js";
import * as errors from "../../../../errors/index.js";
import * as serializers from "../../../../serialization/index.js";
import { Tools } from "../resources/tools/client/Client.js";
import { Prompts } from "../resources/prompts/client/Client.js";
import { Configs } from "../resources/configs/client/Client.js";
import { Chats } from "../resources/chats/client/Client.js";
import { ChatGroups } from "../resources/chatGroups/client/Client.js";
import { Chat } from "../resources/chat/client/Client.js";

export declare namespace EmpathicVoice {
    export interface Options {
        environment?: core.Supplier<environments.HumeEnvironment | environments.HumeEnvironmentUrls>;
        /** Specify a custom URL to connect the client to. */
        baseUrl?: core.Supplier<string>;
        apiKey?: core.Supplier<string | undefined>;
        /** Additional headers to include in requests. */
        headers?: Record<string, string | core.Supplier<string | null | undefined> | null | undefined>;
        fetcher?: core.FetchFunction;
    }

    export interface RequestOptions {
        /** The maximum time to wait for a response in seconds. */
        timeoutInSeconds?: number;
        /** The number of times to retry the request. Defaults to 2. */
        maxRetries?: number;
        /** A hook to abort the request. */
        abortSignal?: AbortSignal;
        /** Additional query string parameters to include in the request. */
        queryParams?: Record<string, unknown>;
        /** Additional headers to include in the request. */
        headers?: Record<string, string | core.Supplier<string | null | undefined> | null | undefined>;
    }
}

export class EmpathicVoice {
    protected readonly _options: EmpathicVoice.Options;
    protected _tools: Tools | undefined;
    protected _prompts: Prompts | undefined;
    protected _configs: Configs | undefined;
    protected _chats: Chats | undefined;
    protected _chatGroups: ChatGroups | undefined;
    protected _chat: Chat | undefined;

    constructor(_options: EmpathicVoice.Options = {}) {
        this._options = _options;
    }

    public get tools(): Tools {
        return (this._tools ??= new Tools(this._options));
    }

    public get prompts(): Prompts {
        return (this._prompts ??= new Prompts(this._options));
    }

    public get configs(): Configs {
        return (this._configs ??= new Configs(this._options));
    }

    public get chats(): Chats {
        return (this._chats ??= new Chats(this._options));
    }

    public get chatGroups(): ChatGroups {
        return (this._chatGroups ??= new ChatGroups(this._options));
    }

    public get chat(): Chat {
        return (this._chat ??= new Chat(this._options));
    }

    /**
     * Send a message to a specific chat.
     *
     * @param {string} chatId
     * @param {Hume.empathicVoice.SendMessageV0EviChatChatIdSendPostRequest} request
     * @param {EmpathicVoice.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Hume.empathicVoice.UnprocessableEntityError}
     *
     * @example
     *     await client.empathicVoice.sendMessageV0EviChatChatIdSendPost("chat_id", {
     *         body: {
     *             "key": "value"
     *         }
     *     })
     */
    public sendMessageV0EviChatChatIdSendPost(
        chatId: string,
        request: Hume.empathicVoice.SendMessageV0EviChatChatIdSendPostRequest,
        requestOptions?: EmpathicVoice.RequestOptions,
    ): core.HttpResponsePromise<void> {
        return core.HttpResponsePromise.fromPromise(
            this.__sendMessageV0EviChatChatIdSendPost(chatId, request, requestOptions),
        );
    }

    private async __sendMessageV0EviChatChatIdSendPost(
        chatId: string,
        request: Hume.empathicVoice.SendMessageV0EviChatChatIdSendPostRequest,
        requestOptions?: EmpathicVoice.RequestOptions,
    ): Promise<core.WithRawResponse<void>> {
        const { accessToken, body: _body } = request;
        const _queryParams: Record<string, string | string[] | object | object[] | null> = {};
        if (accessToken != null) {
            _queryParams["access_token"] = accessToken;
        }

        let _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            this._options?.headers,
            mergeOnlyDefinedHeaders({ ...(await this._getCustomAuthorizationHeaders()) }),
            requestOptions?.headers,
        );
        const _response = await (this._options.fetcher ?? core.fetcher)({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    ((await core.Supplier.get(this._options.environment)) ?? environments.HumeEnvironment.Prod).base,
                `v0/evi/chat/${encodeURIComponent(chatId)}/send`,
            ),
            method: "POST",
            headers: _headers,
            contentType: "application/json",
            queryParameters: { ..._queryParams, ...requestOptions?.queryParams },
            requestType: "json",
            body: _body,
            timeoutMs: requestOptions?.timeoutInSeconds != null ? requestOptions.timeoutInSeconds * 1000 : 60000,
            maxRetries: requestOptions?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
        });
        if (_response.ok) {
            return { data: undefined, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 422:
                    throw new Hume.empathicVoice.UnprocessableEntityError(
                        serializers.empathicVoice.HttpValidationError.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            skipValidation: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                default:
                    throw new errors.HumeError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        switch (_response.error.reason) {
            case "non-json":
                throw new errors.HumeError({
                    statusCode: _response.error.statusCode,
                    body: _response.error.rawBody,
                    rawResponse: _response.rawResponse,
                });
            case "timeout":
                throw new errors.HumeTimeoutError("Timeout exceeded when calling POST /v0/evi/chat/{chat_id}/send.");
            case "unknown":
                throw new errors.HumeError({
                    message: _response.error.errorMessage,
                    rawResponse: _response.rawResponse,
                });
        }
    }

    protected async _getCustomAuthorizationHeaders() {
        const apiKeyValue = await core.Supplier.get(this._options.apiKey);
        return { "X-Hume-Api-Key": apiKeyValue };
    }
}
